# SMTP configuration (optional - required only if you want to send via SMTP/PHPMailer)
# Example (Gmail SMTP):
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-email-password-or-app-password
# SMTP_SECURE=tls

# Destination email (where contact form messages will be sent)
MAIL_TO=sameedjutt234@gmail.com

# SMTP settings for PHPMailer (Gmail SMTP with app password)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=sameedjutt234@gmail.com
SMTP_PASS=rpfb grtj kyfi whwr
SMTP_SECURE=tls

# Optional: set MAIL_FROM to your address to match SMTP user (recommended)
MAIL_FROM=sameedjutt2345@gmail.com

# Enable debug logging to file (path relative to project root). Set empty to disable.
LOG_FILE=assets/php/mail_log.txt
